---
- assert:
    that:
      - platform is defined
      - hostname is defined

- include: base.yml
  tags: "base"

- include: hostname.yml
  tags: "hostname"

- include: sshd.yml
  tags: "sshd"

- include: firewall.yml
  tags: "firewall"

- include: unattended-upgrades.yml
  tags: "unattended-upgrades"
